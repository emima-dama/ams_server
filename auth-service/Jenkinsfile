
pipeline {
    agent any
    triggers {
        cron('* * * * *')
    }
    environment {
        SERVICE_NAME = "AUTH-SERVICE"
        METRICS_ENDPOINT = "/auth-actuator/metrics/tomcat.threads.busy?tag=name:http-nio-auto-1"
        SHUTDOWN_ENDPOINT = "/auth-actuator/shutdown"
    }
    stages { ... }
}